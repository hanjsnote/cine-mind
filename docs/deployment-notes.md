# 배포 플로우 메모

1차 목표: 도커 없이 EC2에서 서비스 오픈(기본 배포 플로우 완성)  
2차 목표: 로컬 개발 환경에 도커 도입  
3차 목표(선택): EC2도 도커 기반으로 재배포

- Render는 콜드 스타트 때문에 사용 X
- EC2는 인스턴스 상시 ON, Redis / PostgreSQL / LLM 호출까지 인프라 통합에 유리
- 현재는 수동 배포 (scp + ssh), 이후 GitHub Actions로 자동 배포 구성